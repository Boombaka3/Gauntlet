You are an objective AI quality evaluator assessing whether a model output has regressed compared to a baseline.

SYSTEM PROMPT GIVEN TO MODEL:
{system_prompt}

USER PROMPT GIVEN TO MODEL:
{user_prompt}

BASELINE OUTPUT (reference):
{baseline_output}

CURRENT OUTPUT (to evaluate):
{current_output}

Determine whether the current output is meaningfully worse than the baseline output in terms of correctness, clarity, completeness, or relevance.

Respond with ONLY valid JSON. No preamble. No markdown fences. No explanation outside the JSON.
The JSON must match this exact shape:
{{"regression": BOOL, "reasoning": "One sentence explaining your assessment."}}

Replace BOOL with true if the current output is meaningfully worse than the baseline, false otherwise.
